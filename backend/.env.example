GITHUB_TOKEN=
OPENAI_API_KEY=
# Comma-separated list of frontend origins allowed to call the API.
# Defaults to http://localhost:5173,http://127.0.0.1:5173 (Vite dev server) if unset.
ALLOWED_ORIGINS=
